import React from 'react';


export default function ActionFormLocation(props) {
    return (
        <div title={ props.location } className="ActionFormLocation">
            { props.location }
        </div>
    );
};
